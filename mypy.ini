[mypy]
ignore_missing_imports = True
check_untyped_defs = True
warn_redundant_casts = True
warn_unused_ignores = True

# Projeto começou sem tipagem nenhuma (achado 2026-07-23) — strict de cara só
# gera ruído. Roda hoje pra pegar bug real (achou 1: api.py usava "log" nunca
# definido). Apertar gradualmente por arquivo conforme for anotando de verdade,
# não configurar strict global de uma vez.
